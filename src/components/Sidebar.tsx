import styled from "styled-components"

export const Sidebar = () => {
    return (
        <SidebarContainer>

        </SidebarContainer>
    )
}

const SidebarContainer = styled.div`
    height: 100%;
    background: #cecece; 
`